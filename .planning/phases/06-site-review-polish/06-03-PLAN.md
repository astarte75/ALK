---
phase: 06-site-review-polish
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/[locale]/investment-platforms/page.tsx
  - src/app/[locale]/investment-platforms/[slug]/page.tsx
  - src/app/[locale]/societa/page.tsx
  - src/app/[locale]/corporate-governance/page.tsx
  - src/app/[locale]/sostenibilita/page.tsx
  - src/app/[locale]/contatti/page.tsx
  - src/app/[locale]/culture/page.tsx
  - src/app/[locale]/privacy/page.tsx
  - src/app/[locale]/cookie-policy/page.tsx
autonomous: false
requirements:
  - REVIEW-01
  - REVIEW-02
  - REVIEW-04

must_haves:
  truths:
    - "All content pages have scroll reveals on sections"
    - "Pages with heroes use AnimatedPageHero"
    - "Section spacing is uniform across all content pages"
    - "All pages render at 320px without horizontal overflow"
    - "Typography on rich-text pages (privacy, cookie, culture) is polished"
  artifacts:
    - path: "src/app/[locale]/investment-platforms/page.tsx"
      provides: "Platforms page with scroll reveals + hero animation"
    - path: "src/app/[locale]/societa/page.tsx"
      provides: "Societa page with scroll reveals"
    - path: "src/app/[locale]/corporate-governance/page.tsx"
      provides: "Governance page with scroll reveals"
    - path: "src/app/[locale]/sostenibilita/page.tsx"
      provides: "ESG page with scroll reveals"
    - path: "src/app/[locale]/contatti/page.tsx"
      provides: "Contact page with scroll reveals"
    - path: "src/app/[locale]/culture/page.tsx"
      provides: "Culture page with scroll reveals"
    - path: "src/app/[locale]/privacy/page.tsx"
      provides: "Privacy page with scroll reveals"
    - path: "src/app/[locale]/cookie-policy/page.tsx"
      provides: "Cookie policy page with scroll reveals"
  key_links:
    - from: "src/app/[locale]/societa/page.tsx"
      to: "src/components/animations/ScrollReveal.tsx"
      via: "import ScrollReveal"
      pattern: "import.*ScrollReveal"
    - from: "src/app/[locale]/investment-platforms/page.tsx"
      to: "src/components/animations/AnimatedPageHero.tsx"
      via: "import AnimatedPageHero"
      pattern: "import.*AnimatedPageHero"
---

<objective>
Wire scroll reveals and visual polish into all remaining content pages: Investment Platforms (listing + detail), Societa, Corporate Governance, Sostenibilita, Contatti, Culture, Privacy, Cookie Policy. Ensure consistent section spacing, typography, and responsive behavior.

Purpose: Complete the scroll reveal coverage across the entire site. No page should feel static or unfinished.
Output: 9 pages upgraded with animations + responsive/typography fixes.
</objective>

<execution_context>
@./.claude/skills/
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-site-review-polish/06-CONTEXT.md
@.planning/phases/06-site-review-polish/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/components/animations/ScrollReveal.tsx:
```typescript
interface ScrollRevealProps {
  children: React.ReactNode
  delay?: number
  direction?: 'up' | 'left' | 'right'
  className?: string
  as?: keyof JSX.IntrinsicElements
}
export function ScrollReveal(props: ScrollRevealProps): JSX.Element
```

From src/components/animations/AnimatedPageHero.tsx:
```typescript
interface AnimatedPageHeroProps {
  imageSrc: string
  title: string
  subtitle?: string
  imagePosition?: string
}
export default function AnimatedPageHero(props: AnimatedPageHeroProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire animations into Investment Platforms, Societa, Governance, ESG pages</name>
  <files>src/app/[locale]/investment-platforms/page.tsx, src/app/[locale]/investment-platforms/[slug]/page.tsx, src/app/[locale]/societa/page.tsx, src/app/[locale]/corporate-governance/page.tsx, src/app/[locale]/sostenibilita/page.tsx</files>
  <action>
**Investment Platforms (page.tsx):**
- Replace static PageHero with AnimatedPageHero
- Wrap each platform section (PE, VC, PIPE) in ScrollReveal
- FundCards already have hover upgrades from Plan 01
- Detail page ([slug]/page.tsx): wrap content sections in ScrollReveal

**Societa (page.tsx):**
- Page uses `page.sections` JSON from Contentful, rendered via PageSections component
- If PageSections renders section blocks, wrap each section block in ScrollReveal
- If page has a hero, replace with AnimatedPageHero
- Timeline section: consider staggered reveals (each timeline item with increasing delay)
- Values/mission sections: ScrollReveal each block

**Corporate Governance (page.tsx):**
- Same pattern as Societa — uses `page.sections` JSON
- Board members, sindaci, control functions sections: wrap each in ScrollReveal
- Tables or lists of members should reveal as groups (not individual rows)

**Sostenibilita (page.tsx):**
- Same pattern — uses `page.sections` JSON
- ESG pillars: each pillar card/block in ScrollReveal with stagger delays
- SFDR section, roadmap section, PDF downloads: ScrollReveal each
- PDF download list: ensure links are clearly clickable with hover state

**Cross-cutting for all 5 pages:**
- Section spacing: consistent `padding: spacing[12] spacing[4]` (or whatever the established pattern is — match Portfolio/Team/News from Plan 02)
- At 320px: no horizontal overflow, readable text, stacked layout
- At 1440px+: centered content, no awkward stretching
- Typography: h1/h2/body consistent with rest of site
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Investment Platforms (listing + detail), Societa, Governance, and ESG pages all have scroll reveals and consistent spacing/typography. No horizontal overflow at 320px.</done>
</task>

<task type="auto">
  <name>Task 2: Wire animations into Contatti, Culture, Privacy, Cookie Policy pages</name>
  <files>src/app/[locale]/contatti/page.tsx, src/app/[locale]/culture/page.tsx, src/app/[locale]/privacy/page.tsx, src/app/[locale]/cookie-policy/page.tsx</files>
  <action>
**Contatti (page.tsx):**
- If page has a hero, replace with AnimatedPageHero
- Wrap contact form section in ScrollReveal
- Wrap office locations section in ScrollReveal (stagger if multiple offices)
- Form fields should be accessible and responsive at 320px
- Office cards: verify map links work, address readable

**Culture (page.tsx):**
- Uses `page.body` rich text from Contentful
- Wrap main content in ScrollReveal
- If page has a hero, use AnimatedPageHero
- Rich text: verify heading hierarchy, image sizing, link colors

**Privacy (page.tsx) + Cookie Policy (page.tsx):**
- Both use `page.body` rich text from Contentful
- These are long legal text pages — apply ScrollReveal to the main content wrapper (single reveal, not per paragraph)
- Typography review: ensure legal text is readable:
  - Proper heading hierarchy (h2, h3 for sections)
  - Adequate line-height (1.7+)
  - `text-align: justify` per convention
  - Links in accentTeal with underline
  - Lists properly indented and styled
- At 320px: text should be readable without horizontal scroll
- At 1440px+: content should have comfortable reading width (not too wide)

**Cross-cutting:**
- Verify all 4 pages have consistent section padding
- Verify navigation to/from these pages works (header/footer links)
- Check both IT and EN content is present
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Contatti, Culture, Privacy, and Cookie Policy pages have scroll reveals. Rich text pages have polished typography (line-height, heading hierarchy, link colors). All pages responsive at all breakpoints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All remaining content pages upgraded with scroll reveals, AnimatedPageHero where applicable, and responsive/typography polish. Investment Platforms, Societa, Governance, ESG, Contatti, Culture, Privacy, Cookie Policy — all 9 pages.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. **Investment Platforms** (`/investment-platforms`):
       - Hero parallax + scroll reveals on platform sections
       - FundCards have hover effects
       - Detail page (`/investment-platforms/[slug]`) is polished
    3. **Societa** (`/societa`):
       - Scroll reveals on timeline, mission, values sections
       - Content complete in IT and EN
    4. **Corporate Governance** (`/corporate-governance`):
       - Board, sindaci, control functions sections reveal on scroll
       - Tables/lists readable at mobile widths
    5. **Sostenibilita** (`/sostenibilita`):
       - ESG pillars, SFDR, roadmap sections reveal
       - PDF downloads visible and clickable
    6. **Contatti** (`/contatti`):
       - Form and offices reveal on scroll
       - Form works at 320px
    7. **Culture** (`/culture`):
       - Rich text content polished, scroll reveal
    8. **Privacy + Cookie Policy** (`/privacy`, `/cookie-policy`):
       - Legal text readable, proper heading hierarchy
       - Links in teal, lists indented
    9. **Quick responsive check:** Resize each page to 320px width — no horizontal overflow anywhere
    10. **Quick EN check:** Verify `/en/societa`, `/en/contatti` have English content
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues per page</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- All pages with heroes import AnimatedPageHero
- All content sections wrapped in ScrollReveal
- No horizontal overflow at 320px on any page
- Rich text pages have consistent typography
</verification>

<success_criteria>
- All 9 content pages have scroll reveals on content sections
- Pages with heroes use AnimatedPageHero with parallax
- Section spacing uniform across all pages
- Typography consistent with Portfolio/Team/News (from Plan 02)
- All pages work at 320px, 768px, 1024px, 1440px, 2560px
- Human approves visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/06-site-review-polish/06-03-SUMMARY.md`
</output>
