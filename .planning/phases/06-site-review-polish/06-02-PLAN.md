---
phase: 06-site-review-polish
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/[locale]/portfolio/page.tsx
  - src/app/[locale]/portfolio/PortfolioGrid.tsx
  - src/app/[locale]/portfolio/[slug]/page.tsx
  - src/app/[locale]/team/page.tsx
  - src/app/[locale]/team/[slug]/page.tsx
  - src/app/[locale]/news/page.tsx
  - src/app/[locale]/news/[slug]/page.tsx
autonomous: false
requirements:
  - REVIEW-01
  - REVIEW-02
  - REVIEW-04

must_haves:
  truths:
    - "Portfolio grid page has scroll reveals on cards and sections"
    - "Team grid page has scroll reveals on cards and sections"
    - "News list page has scroll reveals on cards and sections"
    - "All three listing pages use AnimatedPageHero instead of static PageHero"
    - "Detail pages (portfolio/[slug], team/[slug], news/[slug]) have scroll reveals and typography polish"
    - "All pages render correctly at 320px, 768px, 1024px, 1440px, 2560px"
  artifacts:
    - path: "src/app/[locale]/portfolio/page.tsx"
      provides: "Portfolio listing with AnimatedPageHero + scroll reveals"
    - path: "src/app/[locale]/team/page.tsx"
      provides: "Team listing with AnimatedPageHero + scroll reveals"
    - path: "src/app/[locale]/news/page.tsx"
      provides: "News listing with AnimatedPageHero + scroll reveals"
    - path: "src/app/[locale]/portfolio/[slug]/page.tsx"
      provides: "Portfolio detail with scroll reveals"
    - path: "src/app/[locale]/team/[slug]/page.tsx"
      provides: "Team detail with scroll reveals"
    - path: "src/app/[locale]/news/[slug]/page.tsx"
      provides: "News detail with scroll reveals"
  key_links:
    - from: "src/app/[locale]/portfolio/page.tsx"
      to: "src/components/animations/AnimatedPageHero.tsx"
      via: "import AnimatedPageHero"
      pattern: "import.*AnimatedPageHero"
    - from: "src/app/[locale]/portfolio/PortfolioGrid.tsx"
      to: "src/components/animations/ScrollReveal.tsx"
      via: "import ScrollReveal"
      pattern: "import.*ScrollReveal"
---

<objective>
Wire scroll reveals and AnimatedPageHero into the three most-visited internal page groups: Portfolio, Team, and News (both listing and detail pages). Review and fix responsive layout, typography consistency, and spacing.

Purpose: These are the "money pages" — investors visit portfolio companies, team bios, and news most frequently. They need to feel premium and polished.
Output: 6 pages upgraded with animations + responsive/typography fixes.
</objective>

<execution_context>
@./.claude/skills/
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-site-review-polish/06-CONTEXT.md
@.planning/phases/06-site-review-polish/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/components/animations/ScrollReveal.tsx:
```typescript
interface ScrollRevealProps {
  children: React.ReactNode
  delay?: number
  direction?: 'up' | 'left' | 'right'
  className?: string
  as?: keyof JSX.IntrinsicElements
}
export function ScrollReveal(props: ScrollRevealProps): JSX.Element
```

From src/components/animations/AnimatedPageHero.tsx:
```typescript
interface AnimatedPageHeroProps {
  imageSrc: string
  title: string
  subtitle?: string
  imagePosition?: string
}
export default function AnimatedPageHero(props: AnimatedPageHeroProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire animations into Portfolio, Team, and News listing pages</name>
  <files>src/app/[locale]/portfolio/page.tsx, src/app/[locale]/portfolio/PortfolioGrid.tsx, src/app/[locale]/team/page.tsx, src/app/[locale]/news/page.tsx</files>
  <action>
For each listing page, apply the same pattern:

**Portfolio page (page.tsx):**
- Replace `PageHero` import with `AnimatedPageHero` from `@/components/animations/AnimatedPageHero`
- Since AnimatedPageHero is a client component, it can be used in server component page — Next.js handles the boundary

**PortfolioGrid.tsx (client component):**
- Import `ScrollReveal` from `@/components/animations/ScrollReveal`
- Wrap the filter section (FilterPills) in `<ScrollReveal>`
- Wrap each grid row or the entire grid container in `<ScrollReveal>` — NOT each individual card (too many triggers, stagger looks better at grid level)
- If cards are in a CSS grid, wrap the grid in ScrollReveal. Cards already have their own hover animations from Plan 01.

**Team page (page.tsx):**
- Replace `PageHero` with `AnimatedPageHero`
- If team grid is a client component, import ScrollReveal and wrap the grid section
- If team grid is inline in server component, create a thin client wrapper or wrap the section in ScrollReveal at the grid level

**News page (page.tsx):**
- Replace `PageHero` with `AnimatedPageHero`
- Wrap the news grid/list in ScrollReveal
- Wrap the "load more" / pagination section in ScrollReveal with slight delay

**Responsive review for all three:**
- At 320px: cards should be 1 column, no horizontal overflow, text readable
- At 768px: 2 columns for portfolio/news, 2-3 for team
- At 1024px: 3 columns
- At 1440px: grid maxes out at 1200px centered
- At 2560px: no stretching, content stays centered, no awkward gaps
- Fix any issues found — check paddings, font sizes, grid gaps at each breakpoint

**Typography review:**
- Page titles: consistent size with other pages (3rem mobile, 3.75rem desktop)
- Section headings: h2 should be consistent size across all three pages
- Card text: body font Inter, consistent sizing
- Line heights and letter spacing consistent
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All three listing pages use AnimatedPageHero (with parallax + text reveal) and have ScrollReveal on content sections. Responsive layout works at all 5 breakpoints without overflow or misalignment.</done>
</task>

<task type="auto">
  <name>Task 2: Wire animations into detail pages + typography/spacing polish</name>
  <files>src/app/[locale]/portfolio/[slug]/page.tsx, src/app/[locale]/team/[slug]/page.tsx, src/app/[locale]/news/[slug]/page.tsx</files>
  <action>
Detail pages get the same polish level as listing pages:

**Portfolio detail ([slug]/page.tsx):**
- If page has a hero or header image, consider using AnimatedPageHero or a simpler scroll reveal on the header section
- Wrap main content sections in ScrollReveal: company info, description, metrics, related companies
- Review rich text rendering: check line-height, paragraph spacing, heading hierarchy
- Check image display: company logo/images should have proper aspect ratios, no CLS
- At 320px: single column, images stack, text is readable
- At 1440px+: content centered, comfortable reading width

**Team detail ([slug]/page.tsx):**
- Wrap bio sections in ScrollReveal
- Photo should be properly sized (not stretched or cropped badly) — team photos use `object-position: top`
- Bio rich text: consistent typography, proper heading hierarchy, paragraph spacing
- At 320px: photo stacks above bio, full width
- At 768px+: photo and bio side-by-side or photo at reasonable size

**News detail ([slug]/page.tsx):**
- Wrap article sections in ScrollReveal
- Featured image at top with proper aspect ratio
- Article body rich text: check heading sizes, paragraph spacing, link colors (should be accentTeal), blockquote styling
- Date and category badge at top, consistent with NewsCard styling
- At 320px: full-width image, readable text
- At 1440px+: article body centered with comfortable reading width

**Cross-cutting typography fixes:**
- Ensure all h1 = page title size (3rem → 3.75rem at lg)
- All h2 = section heading size (consistent across pages)
- Body text: Inter, 1rem with 1.7 line-height
- Rich text from Contentful: verify rendered output matches design tokens
- `text-align: justify` on body text per CLAUDE.md convention
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All three detail page types have scroll reveals on content sections. Typography is consistent (heading sizes, body text, rich text rendering). Responsive layout works at all breakpoints. Images display correctly with no CLS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Portfolio, Team, and News pages (listing + detail) upgraded with AnimatedPageHero, ScrollReveal on content sections, premium card hovers, responsive polish, and typography consistency.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. **Portfolio listing** (`/portfolio`):
       - Hero should have parallax on scroll + title fade-in
       - Filter pills and grid should reveal on scroll
       - Cards have premium hover effects (from Plan 01)
       - Resize: 320px, 768px, 1024px, 1440px — no overflow, columns adjust correctly
    3. **Portfolio detail** (`/portfolio/[any-company]`):
       - Content sections reveal on scroll
       - Typography: headings, body text, spacing look premium
       - Company info complete and readable
    4. **Team listing** (`/team`):
       - Hero parallax + scroll reveals on grid
       - Cards hover with photo overlay effect
       - Resize at all breakpoints
    5. **Team detail** (`/team/[any-member]`):
       - Photo + bio layout is polished
       - Scroll reveals on sections
    6. **News listing** (`/news`):
       - Hero parallax + scroll reveals
       - Cards hover with title color change
    7. **News detail** (`/news/[any-article]`):
       - Featured image, article body, date/category all polished
       - Rich text renders with proper typography
    8. Check both Italian (default) and English (`/en/portfolio`, etc.) — content should be present in both
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues per page</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- All listing pages import AnimatedPageHero (not static PageHero)
- ScrollReveal wraps content sections (not individual cards)
- No horizontal overflow at 320px on any of the 6 pages
- Typography heading sizes are consistent across all pages
</verification>

<success_criteria>
- Portfolio, Team, News listing pages all have AnimatedPageHero + scroll reveals
- Portfolio, Team, News detail pages all have scroll reveals + typography polish
- Responsive layout works at 320px, 768px, 1024px, 1440px, 2560px
- Typography hierarchy consistent (h1, h2, body text sizes match across pages)
- Human approves visual quality of all 6 pages
</success_criteria>

<output>
After completion, create `.planning/phases/06-site-review-polish/06-02-SUMMARY.md`
</output>
