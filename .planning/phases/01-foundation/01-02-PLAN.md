---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - middleware.ts
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/messages/it.json
  - src/messages/en.json
autonomous: true
requirements: [FOUND-05]

must_haves:
  truths:
    - "Navigating to / shows Italian content"
    - "Navigating to /en shows English content"
    - "Both locales render as statically generated HTML"
    - "API routes (future) are NOT intercepted by i18n middleware"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "next-intl routing config with localePrefix as-needed"
      contains: "localePrefix"
    - path: "src/i18n/request.ts"
      provides: "Server-side locale resolution"
      contains: "getRequestConfig"
    - path: "middleware.ts"
      provides: "Locale detection and routing middleware"
      contains: "createMiddleware"
    - path: "src/app/[locale]/layout.tsx"
      provides: "Locale-aware layout with NextIntlClientProvider"
      contains: "NextIntlClientProvider"
    - path: "src/messages/it.json"
      provides: "Italian translations"
    - path: "src/messages/en.json"
      provides: "English translations"
  key_links:
    - from: "middleware.ts"
      to: "src/i18n/routing.ts"
      via: "imports routing config"
      pattern: "import.*routing"
    - from: "src/app/[locale]/layout.tsx"
      to: "next-intl/server"
      via: "getMessages for locale"
      pattern: "getMessages"
    - from: "src/app/[locale]/page.tsx"
      to: "next-intl"
      via: "useTranslations hook"
      pattern: "useTranslations"
---

<objective>
Wire next-intl v4 i18n routing so Italian is the default locale (clean URLs without prefix) and English uses /en/ prefix.

Purpose: The i18n routing layer must be in place before any page routes are created in later phases. Getting the middleware matcher and [locale] segment right now prevents cascading issues.
Output: Working bilingual routing with placeholder translations, middleware correctly excluding API routes.
</objective>

<execution_context>
@.planning/phases/01-foundation/01-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 — executor needs these to wire i18n into the existing layout -->

From src/app/layout.tsx (created in Plan 01):
- Root layout with StyledComponentsRegistry wrapping children
- Font CSS variables applied to html element
- GlobalStyle inside registry

From src/styles/theme.ts:
```typescript
export const colors = { bg, surface, border, textPrimary, textSecondary, accentTeal, accentGold } as const
export const fonts = { heading, body } as const
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Configure next-intl routing, middleware, and locale layout</name>
  <files>src/i18n/routing.ts, src/i18n/request.ts, middleware.ts, src/app/[locale]/layout.tsx</files>
  <action>
1. Install next-intl: `npm install next-intl` (if not already installed in Plan 01).

2. Create `src/i18n/routing.ts` using RESEARCH.md Pattern 4:
   ```ts
   import { defineRouting } from 'next-intl/routing'
   export const routing = defineRouting({
     locales: ['it', 'en'],
     defaultLocale: 'it',
     localePrefix: 'as-needed',
   })
   ```

3. Create `src/i18n/request.ts` using RESEARCH.md Pattern 4:
   - Import `getRequestConfig` from `next-intl/server`
   - Import routing config
   - Resolve locale from requestLocale, fallback to defaultLocale
   - Dynamic import messages from `../../messages/${locale}.json`

4. Create `middleware.ts` at PROJECT ROOT (not src/):
   - Import `createMiddleware` from `next-intl/middleware`
   - Import routing from `./src/i18n/routing`
   - Matcher: `['/((?!api|_next|_vercel|.*\\..*).*)']` — CRITICAL: must exclude api routes
   - Export default and config

5. Create `src/app/[locale]/layout.tsx`:
   - CRITICAL: `params` is a Promise in Next.js 15 — must use `const { locale } = await params`
   - Import NextIntlClientProvider from 'next-intl'
   - Import getMessages from 'next-intl/server'
   - Import routing to validate locale, call notFound() if invalid
   - Wrap children in NextIntlClientProvider with messages
   - This is a Server Component (no 'use client')

6. Update `next.config.js` to add the next-intl plugin:
   ```js
   const createNextIntlPlugin = require('next-intl/plugin')
   const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts')
   ```
   Wrap the existing config with `withNextIntl(nextConfig)`.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit</automated>
  </verify>
  <done>next-intl routing configured, middleware excludes API routes, locale layout resolves params with await.</done>
</task>

<task type="auto">
  <name>Task 2: Create bilingual placeholder page with translations</name>
  <files>src/messages/it.json, src/messages/en.json, src/app/[locale]/page.tsx</files>
  <action>
1. Create `src/messages/it.json`:
   ```json
   {
     "home": {
       "title": "Alkemia Capital",
       "subtitle": "Private Equity & Venture Capital",
       "description": "Investiamo in aziende leader nei settori ICT, Automotive, Agri-tech e Food & Beverage."
     },
     "common": {
       "language": "Italiano"
     }
   }
   ```

2. Create `src/messages/en.json`:
   ```json
   {
     "home": {
       "title": "Alkemia Capital",
       "subtitle": "Private Equity & Venture Capital",
       "description": "We invest in leading companies across ICT, Automotive, Agri-tech, and Food & Beverage sectors."
     },
     "common": {
       "language": "English"
     }
   }
   ```

3. Update `src/app/[locale]/page.tsx`:
   - Import `useTranslations` from `next-intl`
   - Use `const t = useTranslations('home')` and `const tc = useTranslations('common')`
   - Render a styled page showing:
     - h1 with `t('title')` — styled with Plus Jakarta Sans, teal accent color
     - h2 with `t('subtitle')`
     - p with `t('description')` — styled with Inter, secondary text color
     - A small badge showing `tc('language')` — gold accent, to prove locale switching works
   - Use styled-components (not inline styles) to verify the full SSR pipeline with i18n
   - Add `generateMetadata` exporting title per locale

4. Verify the full flow works:
   - `npm run build` succeeds (SSG generates both locale paths)
   - `npm run dev` → localhost:3000 shows Italian content
   - `npm run dev` → localhost:3000/en shows English content
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npm run build</automated>
  </verify>
  <done>/ serves Italian placeholder, /en serves English placeholder, both statically generated, styled-components render without hydration issues in both locales.</done>
</task>

</tasks>

<verification>
- `npm run build` generates static HTML for both `/` (IT) and `/en` (EN) routes
- `npm run dev` at `/` shows "Investiamo in aziende leader..."
- `npm run dev` at `/en` shows "We invest in leading companies..."
- No hydration mismatch warnings in console for either locale
- `npx tsc --noEmit` passes with zero errors
</verification>

<success_criteria>
1. Italian content at `/` without locale prefix
2. English content at `/en/` with prefix
3. Both routes statically generated at build time
4. Middleware matcher excludes API routes (verified by pattern)
5. Styled-components SSR works correctly with i18n (no flash, no mismatch)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
