---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.js
  - src/app/layout.tsx
  - src/components/providers/StyledComponentsRegistry.tsx
  - src/styles/GlobalStyle.ts
  - src/styles/theme.ts
  - src/styles/breakpoints.ts
  - .env.local
  - .gitignore
  - .prettierrc
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-06]

must_haves:
  truths:
    - "next dev starts without errors and serves a page at localhost:3000"
    - "No hydration mismatch warnings in browser console"
    - "Page renders with dark background #1A1E22 and no flash of white/unstyled content"
    - "Plus Jakarta Sans renders on headings and Inter on body text"
    - "CSS custom properties are visible on :root in DevTools"
  artifacts:
    - path: "src/components/providers/StyledComponentsRegistry.tsx"
      provides: "SSR style injection for styled-components"
      contains: "useServerInsertedHTML"
    - path: "src/styles/GlobalStyle.ts"
      provides: "CSS reset and :root custom properties"
      contains: "--color-bg"
    - path: "src/styles/theme.ts"
      provides: "Typed theme constants mirroring CSS vars"
      exports: ["colors", "fonts", "spacing"]
    - path: "src/styles/breakpoints.ts"
      provides: "Responsive breakpoint system"
      exports: ["bp", "mq"]
    - path: "next.config.js"
      provides: "styled-components compiler flag"
      contains: "styledComponents: true"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers/StyledComponentsRegistry.tsx"
      via: "wraps children in root layout"
      pattern: "StyledComponentsRegistry"
    - from: "src/app/layout.tsx"
      to: "src/styles/GlobalStyle.ts"
      via: "GlobalStyle inside registry"
      pattern: "<GlobalStyle"
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "font CSS variables on html element"
      pattern: "plusJakartaSans.variable"
---

<objective>
Scaffold the Next.js 15 project with styled-components v6 SSR, dark premium theme via CSS custom properties, font loading, and responsive breakpoint system.

Purpose: Establish the pitfall-free base that every subsequent phase builds on — SSR registry must be wired before any styled-component is written, and dark theme must use CSS vars to prevent FOUC.
Output: Running Next.js 15 app with dark theme, correct fonts, SSR styles, and no hydration mismatches.
</objective>

<execution_context>
@.planning/phases/01-foundation/01-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with styled-components SSR</name>
  <files>package.json, tsconfig.json, next.config.js, src/components/providers/StyledComponentsRegistry.tsx, src/app/layout.tsx, .env.local, .gitignore, .prettierrc</files>
  <action>
1. Run `npx create-next-app@15 . --typescript --eslint --src-dir --app --no-tailwind --no-turbopack --no-import-alias` (scaffold in current directory if empty, otherwise create in a temp dir and move files).
   - If create-next-app does not support `.` as target, scaffold as `alkemia-capital` and move all files to project root.

2. Install dependencies:
   ```bash
   npm install styled-components
   npm install -D prettier sharp
   ```
   Note: Do NOT install @types/styled-components — types are included in v6.

3. Update `next.config.js` (replace default):
   ```js
   /** @type {import('next').NextConfig} */
   const nextConfig = {
     compiler: {
       styledComponents: true,
     },
     images: {
       remotePatterns: [
         {
           protocol: 'https',
           hostname: 'images.ctfassets.net',
         },
       ],
     },
   }
   module.exports = nextConfig
   ```

4. Create `src/components/providers/StyledComponentsRegistry.tsx` using the EXACT official Next.js pattern from RESEARCH.md Pattern 1. The `typeof window !== 'undefined'` guard is mandatory — do not simplify.

5. Update `src/app/layout.tsx`:
   - Import Plus_Jakarta_Sans and Inter from `next/font/google` with `variable` option (see RESEARCH.md Pattern 3).
   - Plus_Jakarta_Sans weights: 400, 500, 600, 700, 800. Inter weights: 400, 500, 600.
   - Both with `display: 'swap'` and `subsets: ['latin']`.
   - Apply font CSS variables to `<html>` element via className.
   - Wrap children in StyledComponentsRegistry with GlobalStyle inside it.
   - Set `<html lang="it">` as default.
   - Keep metadata minimal: title "Alkemia Capital", description placeholder.

6. Create `.env.local` with only `NEXT_PUBLIC_SITE_URL=http://localhost:3000` — NO Contentful keys yet.

7. Create `.prettierrc` with `{ "singleQuote": true, "semi": false, "trailingComma": "all", "tabWidth": 2, "printWidth": 100 }`.

8. Verify `.gitignore` includes `.env.local`, `node_modules`, `.next`, `out`.
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit</automated>
  </verify>
  <done>Project scaffolded, styled-components SSR registry wired in root layout, fonts loading as CSS variables, TypeScript compiles with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create dark theme via CSS custom properties and responsive breakpoints</name>
  <files>src/styles/GlobalStyle.ts, src/styles/theme.ts, src/styles/breakpoints.ts, src/app/[locale]/page.tsx</files>
  <action>
1. Create `src/styles/GlobalStyle.ts` using the EXACT pattern from RESEARCH.md Pattern 2:
   - All colors as CSS custom properties on `:root`:
     - `--color-bg: #1A1E22` (per user decision)
     - `--color-surface: #242A30`
     - `--color-border: #2E363F`
     - `--color-text-primary: #F9FAFB`
     - `--color-text-secondary: rgba(249, 250, 251, 0.6)`
     - `--color-accent-teal: #2EC4B6`
     - `--color-accent-gold: #D4A843`
   - Font tokens referencing CSS variables from next/font: `--font-heading` and `--font-body`
   - Spacing scale (8px base): --space-1 through --space-24
   - CSS reset: box-sizing border-box, margin/padding 0, font-smoothing
   - Body: background-color var(--color-bg), color var(--color-text-primary), font-family var(--font-body)
   - Headings: font-family var(--font-heading), font-weight 700, line-height 1.2
   - Remove default `scroll-behavior: smooth` on html — Lenis will handle this in Phase 5

2. Create `src/styles/theme.ts` — typed constants that mirror CSS vars for TypeScript usage in styled-components:
   ```ts
   export const colors = {
     bg: 'var(--color-bg)',
     surface: 'var(--color-surface)',
     border: 'var(--color-border)',
     textPrimary: 'var(--color-text-primary)',
     textSecondary: 'var(--color-text-secondary)',
     accentTeal: 'var(--color-accent-teal)',
     accentGold: 'var(--color-accent-gold)',
   } as const
   ```
   Export colors, fonts, spacing objects.

3. Create `src/styles/breakpoints.ts` using RESEARCH.md Pattern 5:
   - `bp` object with sm (640px), md (768px), lg (1024px), xl (1280px), 2xl (1536px)
   - `mq` object with media query helpers for styled-components

4. Create a minimal placeholder page at `src/app/[locale]/page.tsx` (without i18n for now — just a styled heading and paragraph to verify theme renders):
   - Use a styled-component for the heading (proves SSR pipeline works)
   - Display text "Alkemia Capital" with Plus Jakarta Sans
   - Display a paragraph with Inter body font
   - Show both accent colors somewhere visible (teal heading, gold accent element)
   - This proves: dark bg renders, fonts load, styled-components SSR works, no FOUC
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Dark theme renders with #1A1E22 background, both fonts visible, accent colors displayed, CSS custom properties visible on :root in DevTools, `next build` succeeds with zero errors and no hydration warnings.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` completes successfully
- `npm run dev` shows dark background (#1A1E22), Plus Jakarta Sans headings, Inter body text
- Browser DevTools shows CSS custom properties on `:root` element
- No "className did not match" warnings in console (hydration OK)
</verification>

<success_criteria>
1. Next.js 15 project builds and runs without errors
2. StyledComponentsRegistry is wired correctly — no hydration mismatches
3. Dark theme visible immediately on page load (no white flash)
4. Both fonts render correctly
5. Responsive breakpoint utilities are available for styled-components
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
