---
phase: 07-investor-portal
plan: 04
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/app/[locale]/investitori/layout.tsx
  - src/app/[locale]/investitori/dashboard/page.tsx
  - src/app/[locale]/investitori/fondi/[slug]/page.tsx
  - src/components/portal/DashboardTable.tsx
  - src/components/portal/NavChart.tsx
  - src/components/portal/LoginForm.tsx
autonomous: false
requirements: [PORTAL-04, PORTAL-06]

must_haves:
  truths:
    - "Portal link is accessible from the main site header (for all users to find the portal)"
    - "Portal UI is responsive from 320px to 2560px without layout breaks"
    - "Number formatting uses correct locale (it-IT with EUR for Italian, en-US with EUR for English)"
    - "Portal passes production build without errors"
    - "All portal pages use ScrollReveal for entrance animations consistent with the rest of the site"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Updated header with portal link"
    - path: "src/components/layout/Footer.tsx"
      provides: "Updated footer with portal link"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "/investitori"
      via: "Navigation link to portal"
      pattern: "investitori|investors"
---

<objective>
Polish the portal: responsive design, portal navigation link in site header/footer, number formatting, animations, and production build verification.

Purpose: Ensures the portal is discoverable from the main site, visually polished at all breakpoints, and production-ready.
Output: Polished, responsive portal integrated into the site navigation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-investor-portal/07-CONTEXT.md
@.planning/phases/07-investor-portal/07-01-SUMMARY.md
@.planning/phases/07-investor-portal/07-02-SUMMARY.md
@.planning/phases/07-investor-portal/07-03-SUMMARY.md
@src/components/layout/Header.tsx
@src/components/layout/Footer.tsx
@src/styles/GlobalStyle.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Site navigation integration + responsive polish + production build</name>
  <files>
    src/components/layout/Header.tsx
    src/components/layout/Footer.tsx
    src/app/[locale]/investitori/layout.tsx
    src/app/[locale]/investitori/dashboard/page.tsx
    src/app/[locale]/investitori/fondi/[slug]/page.tsx
    src/components/portal/DashboardTable.tsx
    src/components/portal/NavChart.tsx
    src/components/portal/LoginForm.tsx
  </files>
  <action>
**1. Add portal link to site navigation:**

Update `src/components/layout/Header.tsx`:
- Add "Area Investitori" / "Investor Portal" link in the header navigation
- Place it as the last nav item OR as a separate styled button/link (distinguish it from public pages)
- Style: subtle border or different color to indicate it's a separate area (e.g., --color-accent-gold text or a small lock icon)
- Use next-intl translations: add `"investitori": "Area Investitori"` to nav keys (if not already in messages, add them)

Update `src/components/layout/Footer.tsx`:
- Add portal link in the footer legal/links section
- "Area Investitori" / "Investor Portal"

Add i18n keys if missing:
- `nav.investitori`: "Area Investitori" (IT) / "Investor Portal" (EN)

**2. Responsive polish for all portal components:**

Review and fix responsive issues in each portal component:

`LoginForm.tsx`:
- Form card: max-width 400px on desktop, full width with padding on mobile
- Input font size 16px minimum on mobile (prevents iOS zoom)
- Test at 320px, 768px, 1024px, 1440px

`DashboardTable.tsx`:
- Desktop: full table layout
- Mobile (below 768px): wrap in a horizontal scrollable container with `-webkit-overflow-scrolling: touch`
- OR switch to card layout on mobile (each position as a card with label-value pairs)
- Fund type badges should not break to new line
- Currency values: use `font-variant-numeric: tabular-nums` for alignment

`NavChart.tsx`:
- ResponsiveContainer handles width automatically
- On mobile: reduce height to 250px
- Reduce margin/padding
- Tooltip: ensure it doesn't overflow viewport on mobile
- X-axis labels: rotate or reduce on mobile if too crowded

`CapitalCallsTable.tsx`:
- Same responsive treatment as DashboardTable
- Horizontal scroll on mobile

`DocumentList.tsx`:
- Cards should stack vertically on all breakpoints
- Download button full-width on mobile

**3. Number formatting consistency:**

Ensure all monetary values use:
```typescript
new Intl.NumberFormat(locale === 'en' ? 'en-US' : 'it-IT', {
  style: 'currency',
  currency: 'EUR',
  minimumFractionDigits: 0,
  maximumFractionDigits: 0,
}).format(value)
```
- Italian: EUR 1.234.567
- English: EUR 1,234,567
- Apply in DashboardTable, CapitalCallsTable, fund detail position cards, NavChart tooltip

For dates:
```typescript
new Intl.DateTimeFormat(locale === 'en' ? 'en-GB' : 'it-IT', {
  day: '2-digit',
  month: 'short',
  year: 'numeric',
}).format(new Date(dateString))
```

**4. ScrollReveal animations (if applicable):**
- Portal pages are behind auth and force-dynamic, so GSAP animations are optional
- If the portal layout supports it, add subtle fade-in on mount for the dashboard content
- Keep it minimal — no scroll-triggered animations needed for data-heavy portal pages
- The login page can have a simple fade-in animation on mount

**5. Production build verification:**
- Run `npm run build` and fix any errors
- Common issues:
  - Recharts may need to be imported with dynamic import if SSR issues arise: `const NavChart = dynamic(() => import('@/components/portal/NavChart'), { ssr: false })`
  - Ensure `export const dynamic = 'force-dynamic'` is on all portal pages
  - Check that `@supabase/ssr` works correctly in production build
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Portal link appears in site header and footer
    - All portal pages are responsive from 320px to 2560px
    - Number formatting is locale-aware (EUR with correct separators)
    - Date formatting is locale-aware
    - Production build passes without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete investor portal — login, dashboard, fund drill-down, site integration, responsive design</what-built>
  <how-to-verify>
    **Full end-to-end flow:**
    1. Run `npm run dev`
    2. From the homepage, find and click the "Area Investitori" link in the header
    3. Login with test credentials
    4. Verify dashboard shows all fund positions with correct EUR formatting
    5. Click into a fund — verify capital calls, NAV chart, documents sections
    6. Click logout — should return to login page
    7. Try accessing `/investitori/dashboard` while logged out — should redirect to login

    **Responsive check:**
    8. Resize to 375px (mobile): login form, dashboard table, and fund detail should be usable
    9. Resize to 768px (tablet): verify layout adapts
    10. Resize to 1440px+ (desktop): full table layout, chart properly sized

    **Bilingual check:**
    11. Switch to English via `/en/investitori` — all labels, column headers, badges should be in English
    12. Number formatting should show correct separators for each locale

    **Security check:**
    13. Open an incognito window, paste a dashboard URL — should redirect to login
    14. Open DevTools Network tab while on dashboard — verify all data requests have auth cookies

    **Production build:**
    15. Run `npm run build` — should complete without errors
  </how-to-verify>
  <resume-signal>Type "portal complete" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes
- Portal is accessible from main site navigation
- Full auth flow works: login -> dashboard -> fund detail -> logout
- Responsive at all breakpoints
- Bilingual in IT and EN
- No data exposure without authentication
</verification>

<success_criteria>
- Portal link visible in site header and footer
- Complete end-to-end investor flow works
- UI is polished and responsive across all breakpoints
- Number/date formatting is locale-correct
- Production build succeeds
- Phase 7 success criteria are all met
</success_criteria>

<output>
After completion, create `.planning/phases/07-investor-portal/07-04-SUMMARY.md`
</output>
