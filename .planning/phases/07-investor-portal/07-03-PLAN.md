---
phase: 07-investor-portal
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/app/[locale]/investitori/dashboard/page.tsx
  - src/app/[locale]/investitori/fondi/[slug]/page.tsx
  - src/components/portal/DashboardTable.tsx
  - src/components/portal/CapitalCallsTable.tsx
  - src/components/portal/NavChart.tsx
  - src/components/portal/DocumentList.tsx
  - src/components/portal/PortalNav.tsx
autonomous: false
requirements: [PORTAL-02, PORTAL-04, PORTAL-06]

must_haves:
  truths:
    - "Logged-in investor sees a table of their fund positions with committed, invested, distributions, and NAV columns"
    - "Clicking a fund row navigates to a drill-down page showing capital calls, NAV chart, and documents"
    - "NAV chart renders as a line chart with quarterly data points"
    - "Documents are downloadable via signed URLs"
    - "All dashboard text is bilingual"
    - "Unauthenticated access to dashboard/fund pages redirects to login"
  artifacts:
    - path: "src/app/[locale]/investitori/dashboard/page.tsx"
      provides: "Dashboard summary page"
      min_lines: 30
    - path: "src/app/[locale]/investitori/fondi/[slug]/page.tsx"
      provides: "Fund drill-down page"
      min_lines: 50
    - path: "src/components/portal/DashboardTable.tsx"
      provides: "Fund positions summary table"
      min_lines: 40
    - path: "src/components/portal/NavChart.tsx"
      provides: "Recharts NAV line chart"
      min_lines: 40
    - path: "src/components/portal/CapitalCallsTable.tsx"
      provides: "Capital calls chronological table"
      min_lines: 30
    - path: "src/components/portal/DocumentList.tsx"
      provides: "Document download list"
      min_lines: 25
  key_links:
    - from: "src/app/[locale]/investitori/dashboard/page.tsx"
      to: "src/lib/supabase/server.ts"
      via: "Server-side data fetch with RLS"
      pattern: "supabase\\.from\\('fund_positions'\\)"
    - from: "src/app/[locale]/investitori/fondi/[slug]/page.tsx"
      to: "src/lib/supabase/server.ts"
      via: "Server-side fetch for calls, NAV, docs"
      pattern: "supabase\\.from\\('capital_calls'\\)|supabase\\.from\\('nav_history'\\)"
    - from: "src/components/portal/NavChart.tsx"
      to: "recharts"
      via: "LineChart component"
      pattern: "import.*LineChart.*recharts"
    - from: "src/components/portal/DocumentList.tsx"
      to: "src/lib/supabase/server.ts"
      via: "Signed URL generation for downloads"
      pattern: "createSignedUrl"
---

<objective>
Build the investor dashboard (summary table) and fund drill-down page (capital calls, NAV chart, documents).

Purpose: This is the core value of the portal — investors see their positions and can drill down into fund details.
Output: Dashboard page with positions table, fund detail page with operations, chart, and document downloads.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-investor-portal/07-CONTEXT.md
@.planning/phases/07-investor-portal/07-RESEARCH.md
@.planning/phases/07-investor-portal/07-01-SUMMARY.md
@.planning/phases/07-investor-portal/07-02-SUMMARY.md
@src/lib/supabase/server.ts
@src/lib/supabase/types.ts
@src/styles/GlobalStyle.ts
</context>

<interfaces>
<!-- From Plan 01 + 02 outputs -->

From src/lib/supabase/types.ts:
```typescript
export type Fund = { id: string; name: string; slug: string; fund_type: 'PE' | 'VC' | 'PIPE'; vintage_year: number | null; currency: string; status: string; }
export type FundPosition = { id: string; investor_id: string; fund_id: string; committed_capital: number; invested_capital: number; distributions: number; current_nav: number; nav_date: string | null; }
export type CapitalCall = { id: string; investor_id: string; fund_id: string; call_date: string; call_type: 'capital_call' | 'distribution' | 'recallable'; amount: number; description: string | null; }
export type NavHistory = { id: string; investor_id: string; fund_id: string; report_date: string; nav_value: number; }
export type InvestorDocument = { id: string; investor_id: string; fund_id: string | null; title: string; document_type: string; storage_path: string; file_size: number | null; uploaded_at: string; }
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient<Database>>
```

From src/app/[locale]/investitori/actions.ts:
```typescript
export async function logout(): Promise<void>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard page with positions summary table</name>
  <files>
    src/app/[locale]/investitori/dashboard/page.tsx
    src/components/portal/DashboardTable.tsx
    src/components/portal/PortalNav.tsx
  </files>
  <action>
Install Recharts:
```bash
npm install recharts
```

Create `src/components/portal/PortalNav.tsx`:
- Client component with portal navigation breadcrumb/tabs
- Show: PortalHeader (logo + logout from Plan 02) at top
- Below header: simple breadcrumb showing current location
- Use `useTranslations('portal')` for labels
- Styled with --color-surface background for nav area

Create `src/components/portal/DashboardTable.tsx`:
- Client component (`'use client'` for potential sorting/interactions later)
- Props: `positions: (FundPosition & { fund: Fund })[]`, `locale: string`
- Render a styled HTML table with columns:
  - Fund name (link to `/investitori/fondi/{fund.slug}`)
  - Fund type badge (PE/VC/PIPE with color-coded pill)
  - Committed capital (formatted as EUR currency with `Intl.NumberFormat`)
  - Invested capital
  - Distributions
  - Current NAV
  - NAV date
- Mobile responsive: horizontal scroll on table, or switch to card layout below md breakpoint
- Empty state: show "Nessuna posizione trovata" / "No positions found"
- Styling:
  - Table: full width, --color-surface background for header row
  - Rows: --color-bg background, --color-border bottom border
  - Hover: subtle --color-surface highlight
  - Fund name: --color-accent-teal link color
  - Fund type badges: PE=teal, VC=gold, PIPE=purple (using existing CSS vars)
  - Currency values: right-aligned, monospace-like font (tabular-nums)
- Use `useTranslations('portal.dashboard')` for all column headers and labels

Create `src/app/[locale]/investitori/dashboard/page.tsx`:
- Server Component (data fetched server-side with RLS)
- `await params` to get locale (Next.js 15 pattern)
- Create Supabase server client
- Call `supabase.auth.getUser()` — redirect to login if no user
- Fetch fund positions: `supabase.from('fund_positions').select('*, fund:funds(name, slug, fund_type, vintage_year, currency)').order('fund_id')`
- RLS automatically filters to current investor
- Fetch investor profile for welcome message: `supabase.from('investors').select('full_name').single()`
- Render:
  - PortalNav with breadcrumb
  - Welcome heading: "Benvenuto, {name}" / "Welcome, {name}"
  - DashboardTable with positions
- Wrap in a container with max-width ~1200px, centered, padding
- Add `export const dynamic = 'force-dynamic'` to prevent SSG (portal must always be server-rendered)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Dashboard page at `/investitori/dashboard` shows fund positions table
    - Table columns match spec: fund name, type, committed, invested, distributions, NAV, date
    - Fund names are clickable links to drill-down pages
    - Data is fetched server-side with RLS filtering
    - Page is force-dynamic (no SSG)
    - All text is translatable
  </done>
</task>

<task type="auto">
  <name>Task 2: Fund drill-down page with capital calls, NAV chart, and documents</name>
  <files>
    src/app/[locale]/investitori/fondi/[slug]/page.tsx
    src/components/portal/CapitalCallsTable.tsx
    src/components/portal/NavChart.tsx
    src/components/portal/DocumentList.tsx
  </files>
  <action>
Create `src/components/portal/CapitalCallsTable.tsx`:
- Client component for potential filtering later
- Props: `calls: CapitalCall[]`, `locale: string`, `currency: string`
- Styled table with columns: Date, Type (badge), Amount (currency formatted), Description
- Sort by date descending (newest first)
- Type badges: capital_call = teal "Richiamo" / "Call", distribution = gold "Distribuzione" / "Distribution", recallable = purple "Richiamabile" / "Recallable"
- Use `useTranslations('portal.fundDetail')` for labels
- Empty state message when no calls

Create `src/components/portal/NavChart.tsx`:
- `'use client'` component (Recharts requires browser APIs)
- Props: `data: { date: string; nav: number }[]`, `fundName: string`, `currency: string`
- Use Recharts: `LineChart`, `Line`, `XAxis`, `YAxis`, `Tooltip`, `ResponsiveContainer`, `CartesianGrid`
- Dark theme:
  - Grid: --color-border stroke with dashed lines
  - Axes: --color-text-secondary for tick labels
  - Line: #2EC4B6 (accent teal), strokeWidth 2
  - Dots: teal fill, active dot gold
  - Tooltip: custom styled component with --color-surface bg, --color-border border
  - Currency formatting in tooltip with `Intl.NumberFormat('it-IT', { style: 'currency', currency: 'EUR' })`
- Y-axis: abbreviated (e.g., "100k", "1M")
- X-axis: formatted dates (Q1 2024, Q2 2024...)
- ResponsiveContainer: width 100%, height 300px
- Empty state when no data

Create `src/components/portal/DocumentList.tsx`:
- Server component (generates signed URLs on server)
- Props: `documents: InvestorDocument[]`, `locale: string`
- For each document:
  - Document title
  - Document type badge (translated)
  - Upload date formatted
  - File size formatted (KB/MB)
  - Download link — use a server action or API route to generate signed URL
  - Download approach: Create an API route at `src/app/api/portal/documents/[id]/route.ts` that:
    1. Verifies auth with `getUser()`
    2. Fetches document record (RLS filters to owner)
    3. Creates signed URL with 60-second expiry
    4. Redirects to signed URL
  - Document list links to `/api/portal/documents/{doc.id}`
- Styled as cards with --color-surface background
- File icon (use a simple SVG or emoji for PDF)
- Empty state message

Also create `src/app/api/portal/documents/[id]/route.ts`:
- GET handler
- Auth check with `getUser()` (return 401 if not authenticated)
- Fetch document metadata from `investor_documents` where id matches (RLS filters)
- If not found, return 404
- Generate signed URL from Supabase Storage (`from('investor-documents').createSignedUrl(path, 60)`)
- Redirect to signed URL

Create `src/app/[locale]/investitori/fondi/[slug]/page.tsx`:
- Server Component
- `await params` to get locale and slug
- Auth check → redirect if no user
- Fetch fund by slug: `supabase.from('funds').select('*').eq('slug', slug).single()`
- If no fund found → notFound()
- Fetch in parallel (Promise.all):
  - Fund position: `fund_positions` where fund_id matches
  - Capital calls: `capital_calls` where fund_id matches, ordered by call_date desc
  - NAV history: `nav_history` where fund_id matches, ordered by report_date asc
  - Documents: `investor_documents` where fund_id matches, ordered by uploaded_at desc
- Render sections:
  1. Back link to dashboard
  2. Fund name heading + type badge + vintage year
  3. Position summary cards (4 cards: committed, invested, distributions, NAV — styled like stat cards)
  4. Capital calls table
  5. NAV chart (transform data: `{ date: formatQuarter(report_date), nav: nav_value }`)
  6. Documents list
- Add `export const dynamic = 'force-dynamic'`
- All sections styled with consistent spacing (--space-12 between sections)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Fund drill-down page renders at `/investitori/fondi/{slug}`
    - Shows position summary, capital calls table, NAV line chart, and document list
    - NAV chart uses Recharts with dark theme styling
    - Documents are downloadable via signed URL API route
    - All data fetched server-side with RLS
    - All text bilingual
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete investor dashboard and fund drill-down pages</what-built>
  <how-to-verify>
    1. Run `npm run dev`
    2. Log in at `/investitori` with test credentials
    3. Verify dashboard shows fund positions table with correct data from seed
    4. Click on a fund name — should navigate to drill-down page
    5. On drill-down page, verify:
       - Position summary cards show correct values
       - Capital calls table shows operations
       - NAV chart renders as a line chart with quarterly points
       - Documents section shows (or shows empty state if no docs seeded)
    6. Check responsive: resize browser to mobile width — tables should scroll or adapt
    7. Check English: navigate to `/en/investitori/dashboard` — all labels should be in English
    8. Open browser DevTools Network tab — verify no fund data requests happen without auth cookie
    9. Try accessing `/investitori/dashboard` in incognito — should redirect to login
  </how-to-verify>
  <resume-signal>Type "dashboard works" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Dashboard loads with real data from Supabase (seed data)
- Fund drill-down shows all 4 sections (summary, calls, chart, documents)
- NAV chart renders correctly with Recharts
- Document download API returns signed URLs
- All pages redirect to login when not authenticated
- All text is bilingual (IT and EN)
- `npm run build` passes
</verification>

<success_criteria>
- Investor sees personalized dashboard with their fund positions
- Drill-down page shows capital calls, NAV chart, and documents
- NAV chart renders as a styled line chart with dark theme
- Documents are downloadable securely
- Portal is fully bilingual
- No data leaks without authentication
</success_criteria>

<output>
After completion, create `.planning/phases/07-investor-portal/07-03-SUMMARY.md`
</output>
