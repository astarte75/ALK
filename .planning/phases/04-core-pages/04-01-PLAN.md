---
phase: 04-core-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/page.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/StatsSection.tsx
  - src/components/sections/NewsPreview.tsx
  - src/components/sections/NewsletterStrip.tsx
  - src/components/cards/NewsCard.tsx
  - src/messages/it.json
  - src/messages/en.json
autonomous: true
requirements: [HOME-01, HOME-05, HOME-06, HOME-07]

must_haves:
  truths:
    - "Homepage renders a full-screen hero with poster image, dark overlay, headline and subtitle"
    - "Homepage shows 4 statistics (20 anni, 270M, 29 operazioni, 18 aziende) with labels"
    - "Homepage shows the latest 3 news cards from Contentful with image, date, title, excerpt"
    - "Newsletter strip displays email input and subscribe button (UI only, non-functional)"
    - "Homepage works in both Italian and English with correct translated text"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Full-screen hero with poster image and overlay"
      contains: "hero-poster.jpg"
    - path: "src/components/sections/StatsSection.tsx"
      provides: "4 stats display reusable on homepage and societa"
      exports: ["default"]
    - path: "src/components/sections/NewsPreview.tsx"
      provides: "Latest 3 news cards section"
      contains: "getNewsArticles"
    - path: "src/components/sections/NewsletterStrip.tsx"
      provides: "Newsletter signup UI strip"
      contains: "input"
    - path: "src/components/cards/NewsCard.tsx"
      provides: "Reusable news article card"
      contains: "NewsArticle"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/sections/HeroSection.tsx"
      via: "import and render"
      pattern: "HeroSection"
    - from: "src/components/sections/NewsPreview.tsx"
      to: "src/lib/contentful/fetchers.ts"
      via: "getNewsArticles(locale, 3)"
      pattern: "getNewsArticles"
    - from: "src/components/sections/NewsPreview.tsx"
      to: "src/components/cards/NewsCard.tsx"
      via: "renders each article as NewsCard"
      pattern: "NewsCard"
---

<objective>
Build the complete homepage with hero section, stats, latest news preview, and newsletter strip — all with real Alkemia content, no placeholders.

Purpose: The homepage is the first page visitors see. It must feel substantial and premium even without Phase 5 animations.
Output: Fully rendered homepage at `/` (IT) and `/en` (EN) with 4 content sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-pages/04-CONTEXT.md
@.planning/phases/04-core-pages/04-RESEARCH.md

@src/app/[locale]/layout.tsx
@src/lib/contentful/fetchers.ts
@src/lib/contentful/types.ts
@src/styles/theme.ts
@src/styles/breakpoints.ts
@src/messages/it.json
@src/messages/en.json

<interfaces>
From src/lib/contentful/fetchers.ts:
```typescript
export const getNewsArticles: (locale: string, limit?: number) => Promise<NewsArticle[]>
export const getSiteConfig: (locale: string) => Promise<SiteConfig | null>
```

From src/lib/contentful/types.ts:
```typescript
// NewsArticle has fields: title, slug, date, category, excerpt, body, featuredImage, externalUrl
// SiteConfig has fields: siteName, tagline, heroVideoUrl, logo, socialLinkedIn, socialTwitter, contactEmail, offices
```

From src/styles/theme.ts:
```typescript
export const colors = { bg, surface, border, textPrimary, textSecondary, accentTeal, accentGold }
export const fonts = { heading, body }
export const spacing = { 1, 2, 3, 4, 6, 8, 12, 16, 24 }
```

From src/styles/breakpoints.ts:
```typescript
export const mq = { sm, md, lg, xl, '2xl' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create homepage section components (Hero, Stats, Newsletter)</name>
  <files>src/components/sections/HeroSection.tsx, src/components/sections/StatsSection.tsx, src/components/sections/NewsletterStrip.tsx, src/messages/it.json, src/messages/en.json</files>
  <action>
1. Create `src/components/sections/HeroSection.tsx` (Server Component):
   - Full-viewport-height section (`min-height: 100vh`) with `position: relative`
   - Background: `next/image` with `src="/images/hero-poster.jpg"`, `fill`, `priority`, `quality={90}`, `style={{ objectFit: 'cover' }}`
   - Dark overlay: `position: absolute` div with `background: linear-gradient(to bottom, rgba(26,30,34,0.4) 0%, rgba(26,30,34,0.8) 100%)`
   - Content centered vertically and horizontally, `position: relative` (above overlay), `z-index: 1`
   - White Alkemia logo at top: `next/image` with `src="/images/alkemia-logo-white.png"`, width 180, height auto
   - Headline: `<h1>` with `useTranslations('home')` for `t('headline')` — large font (4rem mobile, 5.5rem desktop), `fonts.heading`, `colors.textPrimary`, `letter-spacing: -0.02em`
   - Subtitle: `<p>` with `t('subtitle')` — `colors.textSecondary`, max-width 600px, `font-size: 1.125rem`
   - Gold accent line between headline and subtitle (3rem wide, 3px height, `colors.accentGold`)
   - Use `'use client'` since it uses `useTranslations`

2. Create `src/components/sections/StatsSection.tsx` (Client Component for translations):
   - Props: none (stats are hardcoded per user decision, not from Contentful)
   - 4-column grid on desktop, 2-column on mobile
   - Each stat: large number (`fonts.heading`, `colors.accentTeal`, 3rem font-size), label below (`colors.textSecondary`, 0.875rem)
   - Stats data array:
     ```
     { value: '~20', suffix: '', labelKey: 'stats.years' }        // ~20 anni di esperienza
     { value: '270', suffix: 'M', labelKey: 'stats.aum' }         // EUR270M AUM
     { value: '29', suffix: '', labelKey: 'stats.operations' }     // 29 operazioni
     { value: '18', suffix: '', labelKey: 'stats.companies' }      // 18 aziende in portfolio
     ```
   - Currency symbol for AUM: prepend "EUR" before value
   - Section has dark surface background (`colors.surface`), generous vertical padding (space-16)
   - Use `data-stat-value` attributes on number elements (prep for Phase 5 GSAP counters)

3. Create `src/components/sections/NewsletterStrip.tsx` (Client Component):
   - Full-width strip with `colors.accentTeal` background
   - Flexbox: left side has heading text (`t('newsletter.title')`), right side has email input + subscribe button
   - Input: styled text input, placeholder `t('newsletter.placeholder')`
   - Button: styled button with `colors.bg` background, white text
   - Form `onSubmit` just `e.preventDefault()` — UI only per user decision
   - Responsive: stack vertically on mobile

4. Add i18n messages to both `it.json` and `en.json` under `home` namespace:
   ```json
   "headline": "Passione per l'impresa",
   "subtitle": "Alkemia SGR S.p.A. investe in aziende con potenziale di crescita trasformativa, supportandole nel percorso verso l'eccellenza operativa e la leadership di mercato.",
   "stats": {
     "years": "anni di esperienza",
     "aum": "AUM",
     "operations": "operazioni completate",
     "companies": "aziende in portfolio"
   },
   "newsletter": {
     "title": "Resta aggiornato",
     "placeholder": "Il tuo indirizzo email",
     "subscribe": "Iscriviti"
   },
   "newsPreview": {
     "title": "Ultime notizie",
     "viewAll": "Vedi tutte"
   }
   ```
   English equivalents:
   ```json
   "headline": "A Passion for Enterprise",
   "subtitle": "Alkemia SGR S.p.A. invests in companies with transformative growth potential, supporting them on the path to operational excellence and market leadership.",
   "stats": {
     "years": "years of experience",
     "aum": "AUM",
     "operations": "deals completed",
     "companies": "portfolio companies"
   },
   "newsletter": {
     "title": "Stay updated",
     "placeholder": "Your email address",
     "subscribe": "Subscribe"
   },
   "newsPreview": {
     "title": "Latest news",
     "viewAll": "View all"
   }
   ```
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit</automated>
  </verify>
  <done>HeroSection renders full-screen poster with headline/subtitle, StatsSection shows 4 stats in a grid, NewsletterStrip shows email form UI, all i18n messages added for both locales.</done>
</task>

<task type="auto">
  <name>Task 2: Create NewsCard, NewsPreview, and wire homepage</name>
  <files>src/components/cards/NewsCard.tsx, src/components/sections/NewsPreview.tsx, src/app/[locale]/page.tsx</files>
  <action>
1. Create `src/components/cards/NewsCard.tsx` (reusable, used here and on /news page):
   - Props: `{ article: NewsArticle; locale: string }`
   - Card layout: vertical stack — image on top, content below
   - Image: `next/image` with Contentful asset URL (`https:${article.fields.featuredImage.fields.file.url}`), aspect-ratio 16:9 container with `overflow: hidden`
   - Date: formatted with `new Date(article.fields.date).toLocaleDateString(locale === 'it' ? 'it-IT' : 'en-US', { year: 'numeric', month: 'long', day: 'numeric' })`, small font, `colors.textSecondary`
   - Category badge: small pill with `colors.accentTeal` text on transparent bg with border
   - Title: `<h3>` with `colors.textPrimary`, 1.125rem, `fonts.heading`, 2-line clamp
   - Excerpt: `<p>` with `colors.textSecondary`, 0.875rem, 3-line clamp
   - Wrap entire card in Next.js `Link` to `/${locale === 'it' ? '' : locale + '/'}news/${article.fields.slug}`
   - Use the `Link` from `@/i18n/routing` (next-intl createNavigation) if available, otherwise standard next/link with locale prefix logic
   - Hover: subtle translateY(-4px) transition, image slight scale(1.03) transition
   - Card has `colors.surface` background, border-radius 8px, `colors.border` border

2. Create `src/components/sections/NewsPreview.tsx` (Server Component):
   - Props: `{ locale: string }`
   - Fetches `const articles = await getNewsArticles(locale, 3)`
   - Section with heading `t('home.newsPreview.title')` and "View all" link to `/news`
   - 3-column grid on desktop (1-col mobile, 2-col tablet) using `mq` breakpoints
   - Renders 3 `NewsCard` components
   - If no articles returned, render nothing (no empty state needed for homepage preview)
   - Note: This is a Server Component — it fetches data. The heading needs translations, so pass `locale` and use `getTranslations('home')` from `next-intl/server`

3. Rewrite `src/app/[locale]/page.tsx`:
   - Convert to Server Component (remove `'use client'`)
   - Import and render sections in order: HeroSection, StatsSection, NewsPreview, NewsletterStrip
   - Pass `locale` from `params` to NewsPreview
   - Call `setRequestLocale(locale)` for next-intl static rendering
   - Remove all existing placeholder styled-components
   - Wrap sections in a fragment or minimal container (no outer padding — each section handles its own)
   - Import `setRequestLocale` from `next-intl/server`
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Homepage at `/` renders hero with poster image, 4 stats, latest 3 news cards from Contentful, and newsletter strip. English version at `/en` shows translated content. `next build` succeeds.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds with both `/` and `/en` statically generated
- Homepage visually shows: full-screen hero with poster, stats section, 3 news cards, newsletter strip
- Switching locale shows correct translations
- News cards link to `/news/[slug]` (links may 404 until Plan 04-03, that is expected)
</verification>

<success_criteria>
1. Homepage renders 4 distinct sections with real content (no placeholder text)
2. Stats show correct Alkemia numbers (20, 270M, 29, 18)
3. News cards display real Contentful data (image, date, title, excerpt)
4. Page works in both IT and EN
5. `next build` succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-pages/04-01-SUMMARY.md`
</output>
