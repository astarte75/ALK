---
phase: 04-core-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/portfolio/page.tsx
  - src/app/[locale]/portfolio/PortfolioGrid.tsx
  - src/app/[locale]/portfolio/[slug]/page.tsx
  - src/components/cards/PortfolioCard.tsx
  - src/components/filters/FilterPills.tsx
  - src/messages/it.json
  - src/messages/en.json
autonomous: true
requirements: [PORT-01, PORT-02, PORT-03, PORT-04, PORT-05, PORT-06]

must_haves:
  truths:
    - "Visitor sees all 18 portfolio companies in a responsive grid at /portfolio"
    - "Visitor can filter by area (PE/VC/PIPE) using pill buttons"
    - "Visitor can filter by sector using pill buttons that show counts"
    - "Each card shows logo, name, sector, short description, year, and investment type badge"
    - "Clicking a card navigates to /portfolio/[slug] with full company detail"
    - "Cards have CSS hover effects (translateY + image zoom)"
    - "Page works in both IT and EN locales"
  artifacts:
    - path: "src/app/[locale]/portfolio/page.tsx"
      provides: "Server Component that fetches portfolio companies"
      contains: "getPortfolioCompanies"
    - path: "src/app/[locale]/portfolio/PortfolioGrid.tsx"
      provides: "Client Component with filter state and grid rendering"
      contains: "useState"
    - path: "src/app/[locale]/portfolio/[slug]/page.tsx"
      provides: "Company detail page with generateStaticParams"
      contains: "generateStaticParams"
    - path: "src/components/cards/PortfolioCard.tsx"
      provides: "Rich portfolio company card"
      contains: "PortfolioCompany"
    - path: "src/components/filters/FilterPills.tsx"
      provides: "Reusable pill filter component"
      exports: ["default"]
  key_links:
    - from: "src/app/[locale]/portfolio/page.tsx"
      to: "src/lib/contentful/fetchers.ts"
      via: "getPortfolioCompanies(locale)"
      pattern: "getPortfolioCompanies"
    - from: "src/app/[locale]/portfolio/PortfolioGrid.tsx"
      to: "src/components/filters/FilterPills.tsx"
      via: "two FilterPills instances (area + sector)"
      pattern: "FilterPills"
    - from: "src/app/[locale]/portfolio/[slug]/page.tsx"
      to: "src/lib/contentful/fetchers.ts"
      via: "getPortfolioCompanyBySlug"
      pattern: "getPortfolioCompanyBySlug"
---

<objective>
Build the portfolio listing page with dual-axis filtering and individual company detail pages.

Purpose: Portfolio is the most data-rich page and the primary showcase of Alkemia's investments. Filters allow visitors to explore by investment type and sector.
Output: Working `/portfolio` grid with filters and `/portfolio/[slug]` detail pages for all 18 companies.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-pages/04-CONTEXT.md
@.planning/phases/04-core-pages/04-RESEARCH.md

@src/lib/contentful/fetchers.ts
@src/lib/contentful/types.ts
@src/lib/contentful/richText.tsx
@src/styles/theme.ts
@src/styles/breakpoints.ts

<interfaces>
From src/lib/contentful/fetchers.ts:
```typescript
export const getPortfolioCompanies: (locale: string) => Promise<PortfolioCompany[]>
export const getPortfolioCompanyBySlug: (slug: string, locale: string) => Promise<PortfolioCompany | null>
```

From src/lib/contentful/types.ts:
```typescript
// PortfolioCompanySkeleton fields:
// name, slug, description (RichText), shortDescription, sector, investmentType,
// year (Integer), logo (AssetLink), website, featured (Boolean), fundRef, sortOrder
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FilterPills component, PortfolioCard, and portfolio grid page</name>
  <files>src/components/filters/FilterPills.tsx, src/components/cards/PortfolioCard.tsx, src/app/[locale]/portfolio/page.tsx, src/app/[locale]/portfolio/PortfolioGrid.tsx, src/messages/it.json, src/messages/en.json</files>
  <action>
1. Create `src/components/filters/FilterPills.tsx` (Client Component) — reusable across portfolio, team, news:
   - Props: `{ label: string; options: { value: string; label: string; count?: number }[]; value: string; onChange: (value: string) => void }`
   - Horizontal flex-wrap container with `gap: var(--space-2)`, `margin-bottom: var(--space-4)`
   - Each pill: `<button>` with rounded-full shape, border `colors.border` (inactive) or `colors.accentTeal` (active)
   - Active pill: `colors.accentTeal` background, `colors.bg` text
   - Inactive pill: transparent background, `colors.textSecondary` text
   - Hover: `border-color: var(--color-accent-teal)` on inactive pills
   - Show count in parentheses if `count` is defined: `"ICT (6)"`
   - `role="group"` with `aria-label={label}`, each button gets `aria-pressed`
   - Use transient props (`$active`) for styled-components

2. Create `src/components/cards/PortfolioCard.tsx` (used in grid):
   - Props: `{ company: PortfolioCompany; locale: string }`
   - Card with `colors.surface` background, `border-radius: 12px`, `colors.border` border
   - Top section: Logo image using `next/image` with Contentful asset URL, contained in 60px height container, `object-fit: contain`
   - Company name: `<h3>`, `fonts.heading`, 1.125rem
   - Sector label: small text, `colors.textSecondary`
   - Short description: 2-line clamp, `colors.textSecondary`, 0.875rem
   - Bottom row: Year on left, investmentType badge on right (pill shape, `colors.accentTeal` for PE, `colors.accentGold` for VC, `colors.border` bg for PIPE)
   - Wrap in Link to `/portfolio/${company.fields.slug}`
   - Hover effects (CSS only per user decision): card `translateY(-4px)` + subtle `box-shadow` increase, logo container slight `scale(1.03)`, transition 0.3s ease
   - Handle missing logo gracefully (show company name initial in a colored circle)

3. Create `src/app/[locale]/portfolio/page.tsx` (Server Component):
   - `setRequestLocale(locale)` for next-intl
   - Fetch `const companies = await getPortfolioCompanies(locale)`
   - Render page heading and `<PortfolioGrid companies={companies} />`
   - Page heading: `t('portfolio.title')` — "Portfolio" in both locales

4. Create `src/app/[locale]/portfolio/PortfolioGrid.tsx` (Client Component):
   - Props: `{ companies: PortfolioCompany[] }`
   - Two filter states: `area` (default 'all') and `sector` (default 'all')
   - Area options (hardcoded): `[{ value: 'all', label: 'Tutti' }, { value: 'Private Equity', label: 'Private Equity' }, { value: 'Venture Capital', label: 'Venture Capital' }, { value: 'PIPE', label: 'PIPE' }]`
   - Sector options: dynamically built from companies data, with counts that update when area filter changes (see RESEARCH.md Pattern 2)
   - Filter logic: `useMemo` to filter companies by both area and sector
   - When area changes, reset sector to 'all' (avoids empty results)
   - Grid: CSS Grid, `grid-template-columns: 1fr` (mobile), `repeat(2, 1fr)` at `mq.md`, `repeat(3, 1fr)` at `mq.lg`
   - Gap: `var(--space-6)`
   - Render `PortfolioCard` for each filtered company
   - Show filtered count: `{filtered.length} {t('portfolio.companiesCount')}`
   - Use `useTranslations('portfolio')` for labels

5. Add i18n messages:
   IT: `"portfolio": { "title": "Portfolio", "companiesCount": "aziende", "filterArea": "Area di investimento", "filterSector": "Settore", "all": "Tutti" }`
   EN: `"portfolio": { "title": "Portfolio", "companiesCount": "companies", "filterArea": "Investment area", "filterSector": "Sector", "all": "All" }`
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit</automated>
  </verify>
  <done>Portfolio grid page renders all 18 companies with working area and sector filters. FilterPills is reusable. Cards show logo, name, sector, description, year, and investment type badge with CSS hover effects.</done>
</task>

<task type="auto">
  <name>Task 2: Create portfolio detail pages with generateStaticParams</name>
  <files>src/app/[locale]/portfolio/[slug]/page.tsx</files>
  <action>
1. Create `src/app/[locale]/portfolio/[slug]/page.tsx` (Server Component):
   - `generateStaticParams()`: fetch all companies in 'it' locale, flatMap with `routing.locales` to generate `{ locale, slug }` pairs for both IT and EN
   - Page component: `await params` to get `locale` and `slug`, call `setRequestLocale(locale)`
   - Fetch: `const company = await getPortfolioCompanyBySlug(slug, locale)`
   - If `!company`, call `notFound()`
   - Layout — full-width page with generous padding:
     - Back link: arrow + "Torna al portfolio" / "Back to portfolio" linking to `/portfolio`
     - Top section: Logo (large, 120px height container) + Company name `<h1>` + sector + year + investmentType badge
     - Description: Render `company.fields.description` using `renderRichText()` — full rich text body
     - Sidebar info (on desktop, below on mobile):
       - Website link (if `company.fields.website`): external link with icon, `target="_blank"`, `rel="noopener noreferrer"`
       - Sector
       - Investment type
       - Year of investment
       - Fund reference name (if `company.fields.fundRef` is resolved)
     - Use CSS Grid: 2/3 content + 1/3 sidebar on desktop, stack on mobile
   - Styled-components for all layout elements
   - Handle asset fields safely: check `company.fields.logo?.fields?.file?.url` before rendering image
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>Individual company detail pages render at `/portfolio/[slug]` for all 18 companies in both locales. Each page shows company logo, full description, metadata sidebar, and back link. `next build` generates static pages for all 36 paths (18 companies x 2 locales).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds, generates static paths for `/portfolio` + 36 detail pages
- `/portfolio` shows 18 company cards in a 3-column grid
- Clicking PE/VC/PIPE pills filters companies by area
- Clicking sector pills filters by sector with updated counts
- Clicking a card navigates to detail page with full company info
- Both `/portfolio` and `/en/portfolio` render correctly
</verification>

<success_criteria>
1. All 18 companies visible in portfolio grid
2. Area filter (PE/VC/PIPE) works correctly
3. Sector filter shows dynamic counts and filters correctly
4. Company detail pages render rich text descriptions
5. CSS hover effects on cards (translateY + subtle shadow)
6. Both IT and EN locales work
7. `next build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-pages/04-02-SUMMARY.md`
</output>
