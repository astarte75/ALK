---
phase: 04-core-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/team/page.tsx
  - src/app/[locale]/team/TeamGrid.tsx
  - src/app/[locale]/team/[slug]/page.tsx
  - src/components/cards/TeamCard.tsx
  - src/app/[locale]/news/page.tsx
  - src/app/[locale]/news/NewsList.tsx
  - src/app/[locale]/news/[slug]/page.tsx
  - src/messages/it.json
  - src/messages/en.json
autonomous: true
requirements: [TEAM-01, TEAM-02, TEAM-03, NEWS-01, NEWS-02, NEWS-03, NEWS-04, NEWS-05]

must_haves:
  truths:
    - "Visitor sees all 15 team members in a responsive grid at /team with 3:4 portrait photos"
    - "Visitor can filter team by category (Partners/Investment Team/Operations)"
    - "Clicking a team card navigates to /team/[slug] with full profile and bio"
    - "Visitor sees news articles at /news ordered newest-first with image, date, title, excerpt"
    - "Visitor can filter news by category (News/Insights/Events)"
    - "Load more button shows additional articles in batches"
    - "Clicking a news card navigates to /news/[slug] with full article rich text"
  artifacts:
    - path: "src/app/[locale]/team/page.tsx"
      provides: "Team listing page with Server Component data fetch"
      contains: "getTeamMembers"
    - path: "src/components/cards/TeamCard.tsx"
      provides: "Team member card with 3:4 portrait photo"
      contains: "TeamMember"
    - path: "src/app/[locale]/news/page.tsx"
      provides: "News listing page with Server Component data fetch"
      contains: "getNewsArticles"
    - path: "src/app/[locale]/news/NewsList.tsx"
      provides: "Client Component with filter and load-more logic"
      contains: "useState"
    - path: "src/app/[locale]/team/[slug]/page.tsx"
      provides: "Team member detail page"
      contains: "generateStaticParams"
    - path: "src/app/[locale]/news/[slug]/page.tsx"
      provides: "News article detail page"
      contains: "generateStaticParams"
  key_links:
    - from: "src/app/[locale]/team/TeamGrid.tsx"
      to: "src/components/filters/FilterPills.tsx"
      via: "FilterPills for category"
      pattern: "FilterPills"
    - from: "src/app/[locale]/news/NewsList.tsx"
      to: "src/components/filters/FilterPills.tsx"
      via: "FilterPills for category"
      pattern: "FilterPills"
    - from: "src/app/[locale]/news/[slug]/page.tsx"
      to: "src/lib/contentful/richText.tsx"
      via: "renderRichText for article body"
      pattern: "renderRichText"
---

<objective>
Build team listing with category filters and profile detail pages, plus news listing with category filters, load-more pagination, and article detail pages.

Purpose: Team and News are the two other major content grids. They share the FilterPills component from Plan 04-02. Combined in one plan because they follow identical patterns (grid + filters + detail pages).
Output: Working `/team`, `/team/[slug]`, `/news`, `/news/[slug]` pages with real Contentful data.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-pages/04-CONTEXT.md
@.planning/phases/04-core-pages/04-RESEARCH.md

@src/lib/contentful/fetchers.ts
@src/lib/contentful/types.ts
@src/lib/contentful/richText.tsx
@src/styles/theme.ts
@src/styles/breakpoints.ts

<interfaces>
From src/lib/contentful/fetchers.ts:
```typescript
export const getTeamMembers: (locale: string) => Promise<TeamMember[]>
export const getTeamMemberBySlug: (slug: string, locale: string) => Promise<TeamMember | null>
export const getNewsArticles: (locale: string, limit?: number) => Promise<NewsArticle[]>
export const getNewsArticleBySlug: (slug: string, locale: string) => Promise<NewsArticle | null>
```

From src/lib/contentful/types.ts:
```typescript
// TeamMemberSkeleton fields: name, slug, role, category, bio (RichText), photo (AssetLink), linkedIn, isBoard (Boolean), sortOrder
// NewsArticleSkeleton fields: title, slug, date, category, excerpt, body (RichText), featuredImage (AssetLink), externalUrl
```

From src/components/filters/FilterPills.tsx (created in Plan 04-02):
```typescript
interface FilterPillsProps {
  label: string
  options: { value: string; label: string; count?: number }[]
  value: string
  onChange: (value: string) => void
}
export default function FilterPills(props: FilterPillsProps): JSX.Element
```

NOTE: FilterPills is created by Plan 04-02 which runs in parallel (same wave). If 04-03 executes first, create FilterPills inline or copy the implementation from 04-RESEARCH.md Pattern 2. The component at `src/components/filters/FilterPills.tsx` should be identical regardless of which plan creates it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build team grid page with filters and detail pages</name>
  <files>src/components/cards/TeamCard.tsx, src/app/[locale]/team/page.tsx, src/app/[locale]/team/TeamGrid.tsx, src/app/[locale]/team/[slug]/page.tsx, src/messages/it.json, src/messages/en.json</files>
  <action>
1. Create `src/components/cards/TeamCard.tsx`:
   - Props: `{ member: TeamMember; locale: string }`
   - Photo container: 3:4 aspect ratio (`aspect-ratio: 3/4`), `overflow: hidden`, `border-radius: 8px`
   - Photo: `next/image` with `fill`, `sizes="(max-width: 768px) 50vw, (max-width: 1024px) 33vw, 25vw"`, `style={{ objectFit: 'cover' }}`
   - Handle missing photo: show a placeholder div with member initials in `colors.accentTeal` on `colors.surface`
   - Name below photo: `fonts.heading`, 1rem, `colors.textPrimary`
   - Role: `colors.textSecondary`, 0.875rem
   - Wrap in Link to `/team/${member.fields.slug}`
   - Hover: photo `scale(1.05)` with 0.4s transition, name color to `colors.accentTeal`
   - Extract photo URL safely: `member.fields.photo?.fields?.file?.url`

2. Create `src/app/[locale]/team/page.tsx` (Server Component):
   - `setRequestLocale(locale)`, fetch `const members = await getTeamMembers(locale)`
   - Page heading: `t('team.title')`
   - Render `<TeamGrid members={members} />`

3. Create `src/app/[locale]/team/TeamGrid.tsx` (Client Component):
   - Props: `{ members: TeamMember[] }`
   - Filter state: `category` (default 'all')
   - Category options: `[{ value: 'all', label: 'Tutti' }, { value: 'Partners', label: 'Partners' }, { value: 'Investment Team', label: 'Investment Team' }, { value: 'Operations', label: 'Operations' }]` — with counts derived from members
   - Use `FilterPills` component (import from `@/components/filters/FilterPills`). If FilterPills doesn't exist yet (parallel execution), create it here with same interface.
   - Grid: `grid-template-columns: repeat(2, 1fr)` mobile, `repeat(3, 1fr)` at `mq.md`, `repeat(4, 1fr)` at `mq.lg`
   - Gap: `var(--space-6)`
   - Filtered members via `useMemo`

4. Create `src/app/[locale]/team/[slug]/page.tsx` (Server Component):
   - `generateStaticParams()`: fetch all members in 'it', flatMap with `routing.locales`
   - Fetch member by slug, `notFound()` if missing
   - Layout: large photo on left (desktop) or top (mobile), bio on right
   - Photo: `next/image`, max-width 400px, 3:4 aspect ratio
   - Name `<h1>`, role below, LinkedIn link (if present) as icon
   - Bio: `renderRichText(member.fields.bio)` — full rich text
   - Back link to `/team`
   - If `member.fields.isBoard` show a "Board Member" badge

5. Add i18n messages:
   IT: `"team": { "title": "Team", "all": "Tutti", "filterCategory": "Categoria", "backToTeam": "Torna al team", "boardMember": "Membro del CdA" }`
   EN: `"team": { "title": "Team", "all": "All", "filterCategory": "Category", "backToTeam": "Back to team", "boardMember": "Board Member" }`
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit</automated>
  </verify>
  <done>Team grid at `/team` shows all 15 members with 3:4 photos, category filter works, detail pages at `/team/[slug]` show full profile with bio rich text.</done>
</task>

<task type="auto">
  <name>Task 2: Build news listing with load-more and article detail pages</name>
  <files>src/app/[locale]/news/page.tsx, src/app/[locale]/news/NewsList.tsx, src/app/[locale]/news/[slug]/page.tsx, src/messages/it.json, src/messages/en.json</files>
  <action>
1. Create `src/app/[locale]/news/page.tsx` (Server Component):
   - `setRequestLocale(locale)`, fetch `const articles = await getNewsArticles(locale)`
   - Note: fetch ALL articles (no limit) — pagination is client-side via load-more
   - Page heading: `t('news.title')`
   - Render `<NewsList articles={articles} locale={locale} />`

2. Create `src/app/[locale]/news/NewsList.tsx` (Client Component):
   - Props: `{ articles: NewsArticle[]; locale: string }`
   - Filter state: `category` (default 'all')
   - Category options: `[{ value: 'all', label: 'Tutti' }, { value: 'News', label: 'News' }, { value: 'Insights', label: 'Insights' }, { value: 'Events', label: 'Events' }]` — with counts
   - Load-more state: `visibleCount` (default 6), increment by 6 on "Load more" click
   - Filter logic: `useMemo` filters by category, then slice `[0, visibleCount]`
   - When category changes, reset `visibleCount` to 6
   - Grid: `grid-template-columns: 1fr` mobile, `repeat(2, 1fr)` at `mq.md`, `repeat(3, 1fr)` at `mq.lg`
   - Render `NewsCard` for each visible article (import from `@/components/cards/NewsCard` — created in Plan 04-01)
   - NOTE: If NewsCard doesn't exist yet (parallel execution), create it here with the same interface. Props: `{ article: NewsArticle; locale: string }`
   - Load more button: centered below grid, styled with `colors.border` border, `colors.textPrimary` text, hover `colors.accentTeal` border
   - Show button only when `visibleCount < filtered.length`
   - Show count: "{showing} di {total} articoli"

3. Create `src/app/[locale]/news/[slug]/page.tsx` (Server Component):
   - `generateStaticParams()`: fetch all articles in 'it', flatMap with `routing.locales`
   - Fetch article by slug, `notFound()` if missing
   - Layout:
     - Back link to `/news`
     - Featured image at top: full-width, `next/image`, aspect-ratio 16:9 container
     - Category badge + date below image
     - Title `<h1>`, `fonts.heading`, large
     - Article body: `renderRichText(article.fields.body)` — styled with proper typography (headings, paragraphs, links, embedded images)
     - If `article.fields.externalUrl` exists, show "Read original article" external link at the bottom
   - Wrap rich text output in a styled container with good reading typography: max-width 720px, centered, `line-height: 1.7`, paragraph spacing `var(--space-4)`

4. Add i18n messages:
   IT: `"news": { "title": "News", "all": "Tutti", "filterCategory": "Categoria", "loadMore": "Carica altre", "showing": "di", "articles": "articoli", "backToNews": "Torna alle news", "readOriginal": "Leggi l'articolo originale" }`
   EN: `"news": { "title": "News", "all": "All", "filterCategory": "Category", "loadMore": "Load more", "showing": "of", "articles": "articles", "backToNews": "Back to news", "readOriginal": "Read original article" }`
  </action>
  <verify>
    <automated>cd /Users/ricky/MyProjects/Alkemia-HG && npx tsc --noEmit && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>News listing at `/news` shows articles newest-first, category filter works, load-more shows 6 more articles per click, detail pages at `/news/[slug]` render full article with rich text. `next build` generates static paths for all articles in both locales.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds, generates static paths for team and news detail pages
- `/team` shows 15 member cards with 3:4 photos in responsive grid
- Team category filter works correctly
- `/team/[slug]` shows full profile with bio rich text
- `/news` shows articles newest-first with category filter
- Load more button reveals additional articles
- `/news/[slug]` shows full article with rich text body
- Both locales work for all pages
</verification>

<success_criteria>
1. All 15 team members visible with correct photo aspect ratios
2. Team category filter works (Partners/Investment Team/Operations)
3. Team detail pages render bio rich text
4. News articles ordered newest-first
5. News category filter works (News/Insights/Events)
6. Load more pagination works (6 articles per batch)
7. News detail pages render full article body
8. Both IT and EN locales work
9. `next build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-pages/04-03-SUMMARY.md`
</output>
